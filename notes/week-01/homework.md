# Week 1 · Homework

## Before Thursday 10 September

1. Create an account at claude.ai.
2. Subscribe to **Claude Pro**, monthly. ChatGPT Plus subscribers: keep it and install Codex instead (Monday's notes, section 2).
3. Install Python. Windows: [Python 3.13 in the Microsoft Store](https://apps.microsoft.com/detail/9PNRBTZXMB4Z), click Install. Mac: [python.org downloads](https://www.python.org/downloads/), the yellow Download button, then run the installer.
4. Install the agent. Paste the command into a terminal, press Enter, wait for it to finish, then close the window. Windows: PowerShell, the one called "PowerShell", not "PowerShell (x86)". Mac: Terminal (Cmd-Space, type "Terminal", Enter).
   Windows:
   ```bash
   irm https://claude.ai/install.ps1 | iex
   ```
   Mac:
   ```bash
   curl -fsSL https://claude.ai/install.sh | bash
   ```
   Codex: Windows `irm https://chatgpt.com/codex/install.ps1 | iex`, Mac `curl -fsSL https://chatgpt.com/codex/install.sh | sh`.
5. Make a folder named `bpa347` on the Desktop and open a terminal in it. Windows: open the folder, right-click on empty space, **Open in Terminal**. Mac: right-click the folder in Finder, **New Terminal at Folder**; if it is not there, open Terminal, type `cd ` with a space, drag the folder into the window, press Enter.
6. Start the agent and log in: type `claude` (Codex: `codex`), press Enter. A browser tab opens: log in with the account that has Pro. Back in the terminal, answer yes if it asks whether you trust the folder. Then type `/exit`.
7. Read Thursday's notes.
8. Open the [typing app from class](https://bpa347-typing.vercel.app) on your phone and note the next two changes you would ask for.

> [!WARNING]
> **PLEASE NOTE:** Stuck on any of these? Office hours on Wednesday 9 September; time and room on Blackboard.

## Before Monday 14 September

1. Finish the app if class ended before it did: the steps are in Thursday's notes.
2. Open the agent in Thursday's folder, ask it to start the app, then ask for one change.
